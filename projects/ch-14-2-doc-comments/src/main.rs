/// This is a doc comment.
///
/// # Examples
/// ```
/// let x = 5;
/// let y = my_crate::add_one(x);
/// assert_eq!(y, 6);
/// ```
pub fn add_one(x: i32) -> i32 {
    x + 1
}
fn main() {
    println!("Hello, world!");
}
