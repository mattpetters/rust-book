# Chapter notes

```bash

cargo run # build + run project
cargo check # check project produces an executable
cargo build --release # build project

# working on existing projects
$ git clone example.org/someproject
$ cd someproject
$ cargo build

```
